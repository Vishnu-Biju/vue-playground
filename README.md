

# vue-playground

A personal Vue 3 playground with small step-by-step projects to learn and practice core concepts.

This repo is a collection of tiny experiments where I learn Vue piece by piece — starting from `createApp`, then moving to components, props, API calls, etc.

---

## 🧩 What's inside?

Each folder is a small focused mini project:

- `01-counter-app` – basic reactivity, `ref`, `v-model`
- `02-todo-basic` – list rendering with `v-for`
- `03-color-switcher` – class/style binding
- `04-modal-example` – show/hide UI (later)
- `05-api-example` – fetch & render data (later)

> Open any folder and run `index.html` in the browser.

---

## 🛠 Tech stack

- Vue 3 (CDN version)
- HTML + CSS + JS
- No build tools (beginner-friendly)

Optional later:
- Vite
- Single File Components (SFC)
- Routing, state management

---

## ▶️ How to run

1. Clone the repo:

   ```bash
   git clone https://github.com/<your-username>/vue-playground.git
   cd vue-playground

2. Open a project folder:

   cd 01-counter-app

3. Open `index.html`:

* **Double-click** to open in browser
* OR use **VS Code Live Server**
* OR use:
  npx http-server 
  
